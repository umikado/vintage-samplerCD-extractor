# Compiled akaiutil backend (built locally via `vse build`)
third_party/akaiutil/akaiutil
third_party/akaiutil/*.o

# Scratch / extraction artifacts
*.tar
.vse_stereo_tmp_*.wav

# macOS app build output (rebuild via macos-app/Scripts/package-app.sh)
macos-app/.build/
macos-app/dist/

# macOS
.DS_Store
